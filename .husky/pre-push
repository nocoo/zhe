# L2: Integration/API tests
bun run test:api

# G2: Dependency vulnerability scanning (hard gate)
source "$(dirname "$0")/../scripts/ensure-tools.sh"
require_tool osv-scanner "dependency audit"
[ -f bun.lock ] || { echo "❌ bun.lock not found" >&2; exit 1; }
osv-scanner scan --lockfile=bun.lock --config=.osv-scanner.toml
