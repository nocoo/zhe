# osv-scanner configuration
# See: https://google.github.io/osv-scanner/configuration/
#
# Currently empty — no active ignores. Add `[[IgnoredVulns]]` blocks here
# only after confirming the vuln cannot be fixed by upgrading. Always
# include `reason = "..."` and grep the lockfile to confirm the entry
# is still load-bearing on the next release; osv-scanner warns on
# unused ignores during pre-push.
