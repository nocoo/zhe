# Zhe · 本机 Connector 与 X 书签增强

## 用户确认的范围 · 2026-09-13

- 继续在 Zhe 开发；Webhook、手工和 API 创建先按原流程保存书签。
- Connector 合入现有 `zhe connector`，共用 `@nocoo/zhe` 安装、`zhe login` 和 API Key；OpenCLI 作为固定版本依赖随包安装。本机 Connector 定期发现尚未处理的 X 书签，使用已有 OpenCLI / X 登录态补全正文、作者、图片和视频，无需 LLM。
- X 内容融入现有 Basalt 链接卡片、详情和搜索；正常在线时几分钟内自动出现。
- 视频复用 Zhe 现有 R2，使用清楚的用户/来源目录，纳入上传记录、存储管理与孤儿检查；删除书签、附件、文件时正确连锁清理。
- 必须通过现有覆盖率门禁、自动化测试与真实生产链路；完成后移除 Hexly 的 Snail 项目，并清理 Snail 专属 D1、R2、Worker 等资源，最后删除本地 Snail 仓库。GitHub 远程 Snail 仓库保留。

## 基线与边界

- Zhe 开发基线：`d698715`，从干净 `main` 正常 fast-forward；保留 Auth.js / Google、D1 Worker proxy、现有 R2、TypeScript 7.0.2 和公共 Basalt 控件。
- Snail 来源：`6760c56f9ac982440f496f264740252416f128da`；其真实视频/设备/分片/播放验收记录是迁移参考，不是 Zhe 验收。
- 用户本轮明确授权修改 Zhe 和 Hexly 并在验收后删除 Snail，取代此前“只编辑 Snail”的范围限制。Xray、OpenCLI、Basalt 仍只读。
- 仅主 Codex 写代码；按 RED → GREEN → REFACTOR 记录证据。秘密、私人 X 数据、媒体与原始日志只放 ignored 临时目录，不提交；X cookies 不离开本机。
- Hexly 主工作区有其他任务的未提交修改和未推送提交；本任务的删除工作使用隔离 worktree，不发布无关改动。

## 完成情况

1. 已实现并通过本地测试：内容规范化、共享 CLI 身份与独立 Connector scope（30 天期限、用户绑定、撤销），定期发现/租约/重试；纯文本帖子也能独立完成。
2. 已实现并通过本地测试：现有 R2 的有界媒体传输、发布、附件引用、连锁删除及孤儿检查。
3. 已实现并通过桌面/手机验证：Basalt X 卡片/详情、自动刷新、共享 CLI 配置与运行状态。
4. 主体功能已通过本地门禁、独立只读审查和 main CI；`59e5736` 已部署 Railway。生产验收发现的长查询搜索修复已通过本地回归，待 CI 和生产复验。
5. 真实 Google 登录、共享 CLI 登录、Webhook 去重及收藏迁移已完成。正常后台 Connector 在 196 秒内完成目标收藏；视频哈希、Range、自动刷新、实际播放、存储引用、网页/CLI 新建后补全及连锁删除均通过生产验证。
6. Hexly 退役分支 CI 已通过，待发布；Snail D1、R2 和完整 Git 历史已备份。云端和本机资源尚未删除，npm 发布仍需账号二次认证。

## 证据

- C01 RED：`bun x vitest run tests/unit/connector-core.test.ts`，缺失模块，退出 1。GREEN：15 项源 URL、纯文本、长帖、媒体来源测试通过。Refactor：纯数据核心随 CLI 发布，服务器复用，不另建 Connector 包。
- C02 RED：新增真实 SQLite 任务测试，缺失模块退出 1；GREEN：修复并发领取竞争后，10 项发现、租约、用户隔离、过期/撤销、重试与晚到拒绝通过。
- C03 RED：新增媒体模块前退出 1；GREEN：20 项测试通过，包括 SHA/大小/签名、流式 R2 本地后端、文本与视频分别完成、重复上传、视频/海报连锁删除、清理失败重试。
- C04 RED：缺失 HTTP 路由退出 1；GREEN：23 项测试通过，使用 NextRequest + 真实 SQLite 验证共享 API Key、权限/期限、租约、限量 JSON、纯文本 HTTP 完成。
- C05 RED：现有孤儿扫描误判 1 个未发布对象；ScopedDB 删除后对象仍存在，3 项失败。GREEN：26 项测试通过；扫描/清理保护暂存对象，所有 ScopedDB 删除入口统一触发持久清理。
- C06 RED：CLI runner / download 缺失模块退出 1；GREEN：27 项运行器/下载测试通过，共享登录、精准目标、流式上传、媒体失败仍存文本、错误去敏、DNS 和完整解码调用。CLI 已能独立编译；尚需完整覆盖率和安装包验证。
- C07–C09：卡片、刷新和运行状态分别经历行为 RED/GREEN；卡片集成与现有行为共 94 项通过，刷新 4 项通过，运行状态/SQL 30 项通过。Webhook 恢复正式支持，继续只凭 URL token 调用。
- C10 RED：租约跨期、删除附件后重试、图片扩展名共 5 项失败。GREEN：33 项 SQL/R2 测试通过。增加显式附件删除标记，拒绝晚到发布，清理失效上传；提取复杂下载和刷新逻辑后回归通过。
- C11：OpenCLI 隔离进程/适配器 18 项、运行器 13 项通过；CLI 总计 220 项通过，语句 95.74%、分支 91.94%、函数 95.48%、行 96.90%，超过原有门禁。仅适配器合同与合成数据验证，尚非真实 X 验收。
- C12/C15：真实 HTTP 首轮 208/209，通过保存/领取/文本及媒体写入，Range 因本地 R2 shim 未实现而失败；补齐流式 Range 后暴露 D1 非递归触发器差异：海报上传记录未删除。单测改为 `recursive_triggers=OFF` 复现 RED，再显式删除关联海报。GREEN：完整 L2 27 文件 / 209 项通过（真实 Miniflare D1、HTTP、文件系统 R2）；SQL/上传回归 68 项通过。
- C14：上传列表支持公共 Basalt 视频预览、操作按钮和连锁删除说明；存储行使用实际 publicUrl；RED 2 项后，相关组件测试通过。
- C16：新增 23 项服务器 capture 信任边界检查通过。新表在 Drizzle schema 中同步；CLI 独立构建、lint 和覆盖率加入 CI。
- C17/C20：新桌面 1365px 与手机 390px 场景均通过真实 HTTP 上传、15 秒自动刷新、正文/引用、图片预览、视频播放进度及删除后 R2 404；随后完整 L3 159 项通过。侧栏折叠 logo 不跳动。
- C19 RED：临时目录创建失败会遗留 heartbeat、零/负视频尺寸仍被接受，3 项 CLI 行为测试失败；Webhook 导航仍有 Legacy，1 项组件测试失败。GREEN：修正资源清理与尺寸验证，CLI 223 项通过，语句 95.89%、分支 92.18%、函数 95.48%、行 97.06%；Sidebar 68 项通过。完整 Biome 638 文件通过。会话产物排除检查但保持 ignored。
- C22：真实 OpenCLI 使用本机既有 X 会话读取获准迁移的原帖，获得正文和视频；首次下载 DNS 超时，重试成功。视频 1,729,313 字节与 Snail 既有验收 SHA-256 完全相同，FFmpeg 完整解码通过，海报生成成功。未进行全量 X 书签同步，cookies 未传至服务器；Zhe R2 生产写入仍待验证。
- C23：CLI tarball 独立安装成功，`zhe connector --help` 正确列出五个子命令；Worker 72 项通过。完整 Worker 安装锁文件已复核。
- C24：独立审查发现短正文截断和重试反馈不更新，9 项新行为测试 RED 后修复，卡片 14 项 GREEN；短正文无展开按钮时不裁剪，重试反馈随远端状态变化失效，失败后可再次重试。
- C24 L3：桌面七行短文和手机 270 字中文正文均验证完整可见，3 项浏览器测试通过，包含原先长文、播放和删除场景。
- C26 RED：独立审查的原 URL 替换竞态、第五次领取后中断、已有备注隐藏，4 项测试失败。GREEN：领取 CAS 校验 source_url，耗尽次数的过期租约转为可手工重试状态，Basalt 卡片保留备注；SQL/卡片合计 51 项通过。CLI 安全扫描发现既有 PostCSS 传递依赖漏洞，升级锁定下限后扫描通过。
- C27：补充媒体失败重试、共享密钥到期/撤销及 R2 streaming 行为测试后，主应用 unit-only 覆盖率门禁通过：161 文件 / 3037 项，语句 95.43%、分支 88.55%、函数 93.12%、行 96.51%。Integration 193 项、CLI 223 项、真实本地 HTTP 209 项通过；类型、Biome、构建及 root/CLI OSV 扫描通过。
- C28：最终完整 L3 共 159 项通过，包括桌面/手机正文可见性、自动补全刷新、实际视频播放、图片预览和连锁删除后 R2 404。独立 Worker 72 项及 `worker/wrangler.local.toml` dry-run 通过。
- C30：已导出并在内存 SQLite 中成功恢复两份生产 D1 备份，文件权限 0600。Snail 共 1 个资产、1 个收藏、1 个 blob，无分类或标签；Zhe 备份含 219 条链接、12 个上传记录。生产 Connector 新表在备份时尚不存在。只记录数量，私人内容不进入文档。
- C34 HTTP RED：独立 Codex 审查复现 Next.js 通用代理将 12 MiB 上传截成 10 MiB；新增真实 HTTP 大文件场景因 `digest_mismatch` 失败。GREEN：Connector API 由已有路由认证独立处理，避免通用代理复制/截断正文，完整 HTTP 210 项通过；未登录 401、错误权限 403 的检查仍通过。
- C34 存储 RED：新增真实 SQLite / 本地 R2 的删除竞态与队列测试共 4 项失败。GREEN：预约 INSERT 原子核对删除标记；清理请求使用单调递增的时间戳区分代次，旧删除只确认其领取的代次；队列在 LIMIT 前排除仍被引用的文件。媒体/队列/上传列表/代理回归 96 项通过，避免附件被重试恢复、晚到上传丢失清理记录，以及 100 个受保护对象堵塞后续清理。
- C35 界面 RED：删除视频后，文件列表仍显示已被服务器连锁删除的海报。新增行为测试失败后，让成功删除重新读取服务端文件列表；37 项上传 ViewModel 测试通过。桌面/手机 L3 改为从真实文件页面确认删除，验证 3 个附件立即只剩 1 张独立图片，原书签中的视频随轮询消失；3 项浏览器测试通过。
- C38 GIF RED：独立 Codex 复审发现常见 `tweet_video` GIF 路径被过滤；新增规范化、下载与服务端接收场景共 3 项失败。GREEN：在共享 URL 校验中补齐精确的 GIF 视频和缩略图路径，保留 CDN 主机、文件名、协议、查询参数和媒体身份边界；CLI 226 项及服务端 capture 24 项通过，CLI 覆盖率门禁通过。继续复用现有下载、完整解码和发布流程，无独立 GIF 实现。
- C39/C45：独立只读 Codex 审查至 `59e5736` Sign Off，无 P0/P1/P2/P3。验证分支 CI `34733337809` 和 main CI `34733681798` 均成功；Railway 部署 `6d5b8e0e-1458-4b7f-a0a6-3519c204bb32` 确认来自同一 SHA，公共 `/api/live` 返回 JSON 200，未认证 Connector 请求返回 401。
- C40/C41：再次导出生产 Zhe D1 并成功恢复内存 SQLite，随后应用 `0024_add_x_connector.sql`。生产 4 张新增表和 4 个触发器核验通过，原有 219 条链接和 12 个上传记录在迁移时不变；未重复执行 migration。
- C46/C48：通过真实 Google 会话创建共享 CLI/API Key，再运行正常 `zhe login`，进程退出 0，配置权限 0600。外部 Webhook POST 不带 Authorization 或 Cookie，重复请求复用书签，保留收藏标签；启动 Connector 前页面显示已保存、等待补全。验收脚本最初未关闭管道 stdin，修正脚本后重用同一密钥完成登录，未更改认证实现。
- C51：正常 LaunchAgent 轮询真实 OpenCLI，目标收藏首次领取即完成，用时 196 秒。生产 R2 视频 1,729,313 字节与来源 SHA-256 一致，Range 206、海报 200；原 Dashboard 未 reload 即出现视频，浏览器实际播放至 1 秒，720×1280，无媒体错误或横向溢出。存储管理将视频和海报标记为 linked，不可作为孤儿勾选。脚本移除了浏览器桥不支持的聚焦命令；应用无需修正。
- C56：通过普通网页和已安装 CLI 分别创建临时 X 链接，正常后台轮询自动补全。真实文件界面删除视频后海报立即消失；API 删除另一书签后媒体和 uploads 引用连锁清理。四个临时媒体 URL 均为 404，删除队列清空，临时链接已移除，最终收藏与原视频保留。
- C53：再次核对 Snail 专属资源：2 个 Access app、1 个 Worker custom domain、1 个 D1、1 个 R2 bucket（2 对象，共 1,888,128 字节），无专属 Access service token。两个 R2 原对象已完整备份并校验，Git bundle 包含完整历史；Zhe 与 Hexly 共享资源明确排除。
- C54 搜索 RED：真实生产查询完整 URL 返回 500，D1 明确报 `LIKE or GLOB pattern too complex`；官方限制为模式最多 50 字节。新增真实 SQLite 行为测试中，长查询及字面 `%`/`_` 两项失败。GREEN：共享链接查询改用参数化 `instr(lower(...), lower(?))`，保留用户范围和大小写行为，4 项真实 SQLite 及相关 ScopedDB/集成回归共 194 项通过。没有截断 URL 或查询内容。
- C55：搜索修复后完整 unit-only 162 文件 / 3047 项通过，语句 95.43%、分支 88.58%、函数 93.06%、行 96.52%；真实本地 HTTP 27 文件 / 211 项通过，包含长 URL 和中文查询。搜索及 Connector 桌面/手机浏览器回归 11 项通过；类型、Biome、生产构建通过。原始日志位于 ignored `.artifacts/`，私人源数据、凭据和备份位于 ignored `.connector/`。
